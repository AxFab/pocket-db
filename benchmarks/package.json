{
  "name": "pocket-db-benchmarks",
  "private": true,
  "version": "0.0.0",
  "description": "Comparative benchmarks for pocket-db vs SQLite, a naive JSON-file store, lowdb, and LokiJS. Not published.",
  "type": "module",
  "scripts": {
    "bench": "node --import tsx bench.ts",
    "bench:large": "node --import tsx large-scale.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "lokijs": "^1.5.12",
    "lowdb": "^7.0.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/lokijs": "^1.5.14"
  }
}
