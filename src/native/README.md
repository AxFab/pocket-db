# Native Boundary

This directory is reserved for future native C integration.

The TypeScript API should depend on interfaces rather than native details so the
initial storage engine can remain pure TypeScript while leaving room for a C
backend later.
