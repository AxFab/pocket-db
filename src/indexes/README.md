# Indexes

This directory will contain index definitions and index maintenance code.

The first index type should be a simple single-field index. Compound indexes,
text search, and advanced collation rules should wait until the storage model is
stable.
